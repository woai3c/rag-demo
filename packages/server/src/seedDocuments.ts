export interface SeedDocument {
  source: string;
  content: string;
}

export const SEED_DOCUMENTS: SeedDocument[] = [
  {
    source: 'RAG 基础知识（内置）',
    content: `# RAG 基础知识

## 定义

RAG 是 Retrieval-Augmented Generation 的缩写，中文通常译为“检索增强生成”。完整流程先从外部知识库检索与问题相关的资料，再把资料和问题交给大语言模型生成回答。

## 三个阶段

索引阶段负责解析文档、分块、生成向量并保存索引。检索阶段负责根据用户问题找出 Top-K 相关片段。生成阶段负责让大语言模型依据这些片段组织答案并标注来源。

## 数据更新

RAG 的外部知识库可以独立更新，不需要每次修改资料都重新训练大语言模型。资料改变后，需要重新生成受影响片段的 Embedding，并更新检索索引。

## 向量数据库

RAG 需要检索器，但不强制使用专用向量数据库。小型应用可以把向量放在内存、JSON 文件、IndexedDB 或 SQLite 中；规模和并发增加后，可以使用 pgvector、Qdrant、Milvus、Weaviate 或 Pinecone 等方案。`,
  },
  {
    source: '混合检索说明（内置）',
    content: `# 混合检索

## 向量检索

向量检索使用 Embedding 模型把问题和文档片段转换为向量，再用余弦相似度等指标排序。它擅长处理同义表达和自然语言概念查询。

## BM25

BM25 是经典的关键词相关度排序函数。它综合考虑查询词在当前文档中的词频、该词在全部文档中的稀有程度，以及文档长度。错误码、产品编号、函数名等精确词项通常适合 BM25。

## 加权融合

向量相似度和 BM25 分数的范围不同，不能直接相加。本 Demo 先分别归一化，再计算 0.7 × 向量分 + 0.3 × BM25 分。权重不是概率，也不是所有业务的固定最优值，应通过有标准答案的查询集调优。`,
  },
  {
    source: '星河科技差旅与交通报销制度（虚构）',
    content: `# 星河科技差旅与交通报销制度

> 本资料是 RAG Demo 使用的虚构教学数据，不代表任何真实公司的制度。

## 市内交通费用

员工因公产生的市内交通费用，每月报销限额为 800 元。超出限额的部分需要部门负责人审批，并在报销单中说明业务原因。

## 票据期限

交通费用应在费用发生后 30 天内提交有效票据。超过 30 天的票据需要财务负责人额外审核。

## 住宿标准

上海地区住宿标准为每晚不超过 600 元，北京地区为每晚不超过 650 元。超出标准需要在出差前提交申请。

## 报销入口

员工登录 FinanceHub 后，在首页选择“费用报销”，再选择交通、住宿或其他费用类别。系统错误码 ERR_FIN_403 表示当前账号没有报销权限，需要联系部门管理员。`,
  },
  {
    source: 'Demo 生成模型配置（内置）',
    content: `# Demo 的生成模型配置

## 工作方式

本 Demo 的 Embedding 由服务端本地 ONNX 模型完成。完整 RAG 问答会把用户问题和检索得到的 Top-K 片段发送给环境变量中选择的大语言模型。

## 支持的厂商

服务端提供 DeepSeek、阿里云百炼 Qwen、智谱 GLM、Kimi Moonshot、火山方舟豆包和百度千帆 ERNIE 的配置预设，也支持自定义 OpenAI-compatible 服务。

## API Key

API Key 只保存在服务端 .env 文件中。浏览器状态接口只显示厂商名称、模型名称和是否配置完成，不返回 API Key。`,
  },
];
